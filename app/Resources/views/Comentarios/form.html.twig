{% extends '::base.html.twig' %}

{% block body %}
  <div>
    <form method="post" action="{{ action }}" novalidate="novalidate">
      <!-- para evitar la validacion html5 -->

      <div class="btn-primary">{{ form_label(form.texto) }}:
        {{ form_widget(form.texto) }}</div>
      {{ form_rest(form) }}
      <div>
        <button class="btn-success" type="submit" value="submit">Añadir</button>
      </div>

    </form>
  </div>
{% endblock %}
